
pub struct Print {
    debug: bool,
}

impl Print {
    // TODO: finish this struct/class, which prints only if debug=true (to make functions in this crate,
    //   typically littered with print statements, perform their stuff silently)
    //  This idea would be useful, but it involves variadic interfaces and macro definitions...so maybe later.

    // fn println(format: &str, )
}